import{describe,expect,it}from'vitest';import{travelStatus}from'./alertStatus';describe('travel alert status',()=>{it('distinguishes full and partial zero-alert coverage',()=>{expect(travelStatus([],'FULL')).toBe('OK');expect(travelStatus([],'PARTIAL')).toBe('DA CONTROLLARE')});it.each(['warning','critical'])('renders attention for %s',level=>expect(travelStatus([{level}],'FULL')).toBe('ATTENZIONE'));it('keeps info explainable without escalating',()=>expect(travelStatus([{level:'info'}],'FULL')).toBe('OK'))});
